/* Initial beliefs and rules */

/*Initial goals */

!start.

/* General Plans */

+!start : true
	 <- .print("Hello from me too");
            !deliver(111,10,20).

+moved : true <- .print("Received moved signal").

+grasped : true <- .print("Received grasped signal").

+released : true <- .print("Receved released signal").<- .print("Time to move product ", PR, "from location ", SRC, " to location ", DST);
	move(PR,SRC)[artifact_name(r1)];
	grasp(PR)[artifact_name(r1)];
	move(PR,DST)[artifact_name(r1)];
	release(PR)[artifact_name(r1)].

{ include("$jacamoJar/templates/common-cartago.asl")} 
